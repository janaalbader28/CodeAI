function calculateTotal(price, tax) {
  let total = price + tax

  if (total > 100) {
    console.log("High total"
  else {
    console.log("Normal total");
  }

  return total;
}

let result = calculateTotal(90, 15;
console.log("Result is:", result);
